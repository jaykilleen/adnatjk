var shifts = document.querySelector("#shifts")
var flashes = document.querySelector("#flashes")

flashes.innerHTML = "<%= escape_javascript(render partial: 'flashes' ) %>"
shifts.innerHTML = "<%= escape_javascript(render partial: 'table' ) %>"
